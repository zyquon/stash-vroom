name: Stash-VRoom
# requires: PythonDepManager
description: HereSphere support for browsing and viewing VR videos
version: 0.1.0
url: https://zyquon.github.io/stash-vroom/
ui:
  requires: 
  - stashUserscriptLibrary
  javascript:
  - stash-plugin/js/stash-vroom.js
# TODO
# can be BOOLEAN, NUMBER, or STRING
# settings:
#   vroomTest:
#     displayName: Stash-VRoom Test
#     type: STRING
exec:
  - python
  - "{pluginDir}/stash-plugin/start.py"
interface: raw
tasks:
  - name: Run HereSphere server
    description: Start the HereSphere server to browse and view VR videos
    # defaultArgs:
    #   name: foo
    #   baz: null